using System.Collections.Generic;
using UnityEngine;

public class JatekosMozgas : MonoBehaviour
{

    public Rigidbody jatekosTest;

    // Start is called before the first frame update
    void Start()
    {
        jatekosTest.AddForce();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
